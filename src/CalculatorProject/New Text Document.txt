things need to update in calculator app

1. make the app responsive
2. make the second value visible in the place of first value[done]
3. make the input changeable using keyboard[done]


1. apply hide properties in the secondvalue field by default [done]
2. look for the operator value if its blank then do nothing if its not blank then first remove the hide field properties from 
second field and apply to firstvalue[done]
3. then look for the onclick of = operator then again set hide properties to secondvalue field[done]